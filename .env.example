# Finance Tracker Environment Example

# --- Core App Secrets ---
# Random 24+ byte hex for Dash/Flask session signing
SECRET_KEY=replace-with-generated-hex

# Primary Google Sheet backing the dashboards
GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit

# Optional path override for the Google service account JSON used by gspread
GOOGLE_SERVICE_ACCOUNT_JSON=/absolute/path/to/finance-tracker-service-account.json

# Landing folders
INCOMING_FOLDER_ID=drive-folder-id-for-incoming-uploads
PROCESSED_ROOT_FOLDER_ID=drive-folder-id-for-processed-root


# Linkwarden instance used by the Quick Links tab
LINKWARDEN_URL=http://localhost:3000

# Legacy fallback for Quick Links; remove when all users migrate
# to auth
QUICKLINKS_PASSWORD=XXXXXXXX

# --- QuickBooks OAuth Credentials ---
# Copy from Intuit Developer → Keys & OAuth
QB_CLIENT_ID=your-intuit-client-id
QB_CLIENT_SECRET=your-intuit-client-secret

# Must match the redirect URI registered on the Intuit app
QB_REDIRECT_URI=http://localhost:8765/callback

# Numeric Company/Realm ID tied to the QuickBooks company file
QB_REALM_ID=XXXXXXXXXX

# Development (sandbox) or Production
QB_ENV=Development

# Default QuickBooks item used for invoice line items (must exist in QBO)
QB_DEFAULT_ITEM_ID=1
QB_DEFAULT_ITEM_NAME=Services - Consulting

# Optional overrides for sync behavior
QB_ELIGIBLE_STATUSES=submitted,pending
QB_MINOR_VERSION=70

# --- Sync Feature Toggles ---
# invoice_only (Simple Start) or dual (Invoices + Bills)
QB_SYNC_MODE=invoice_only

# Enable bill syncing after upgrading to Essentials/Plus
QB_ENABLE_BILLS=false

# Optional chart-of-accounts defaults for vendor bills
QB_DEFAULT_VENDOR_ACCOUNT=
QB_DEFAULT_EXPENSE_ACCOUNT=
QB_AUTO_MARK_BILL_PAID=false

# Default QuickBooks item (Product/Service) used when creating invoices.
# Item name defaults to the sheet's `invoice` column, but the ItemRef value must
# point to an existing Product/Service inside QuickBooks.
QB_DEFAULT_ITEM_ID=
QB_DEFAULT_ITEM_NAME=Services
